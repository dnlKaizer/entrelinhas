// Dentro do BookCategory.tsx
<Flex gap="large" wrap="wrap">
  {books.map((book) => (
    <BookCard 
      key={book.id}
      id={book.id}
      title={book.title}
      author={book.author}
      cover={book.cover}
      rating={book.rating}
      // Se o título da categoria for "Já li:", mostramos as estrelas
      showRating={title === "Já li:"} 
    />
  ))}
</Flex>